/**
 * This model represents a patent preview object used in the patent preview component
 */
export interface PatentPreview {
    id: string;
    title: string;
    subTitle: string;
    mainText: string;
    showSave: boolean;
}
